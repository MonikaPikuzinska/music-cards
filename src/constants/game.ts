/** Recommended party size from the instructions. */
export const RECOMMENDED_PLAYERS_MIN = 4;
export const MAX_PLAYERS = 6;

/**
 * Minimum online players before the Master can start picking.
 * 2 keeps a Master + one player session playable; 4–6 is the intended size.
 */
export const MIN_PLAYERS_TO_START = 2;

/** Shared countdown length for select and vote phases (seconds). */
export const GAME_TIMER_DURATION_SEC = 120;

/** Each player is dealt this many unique songs. */
export const HAND_SIZE = 6;
